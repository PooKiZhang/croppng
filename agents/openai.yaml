interface:
  display_name: "Reference PNG Redraw"
  short_description: "参考图 AI 重绘并输出透明 PNG"
  brand_color: "#FF5CA8"
  default_prompt: "Use $reference-png-redraw to turn this local crop into a clean transparent PNG asset."
policy:
  allow_implicit_invocation: true
